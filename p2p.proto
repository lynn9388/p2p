syntax = "proto3";

import "google/protobuf/wrappers.proto";

package p2p;

// Peers is a list of peer's network address
message Peers {
    repeated string Peers = 1;
}

service NodeService {
    rpc GetPeers (google.protobuf.StringValue) returns (Peers);
}
